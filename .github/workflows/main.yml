name: Build SKYNET GPS Bridge APK

on:
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Java 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3

      - name: Install Android SDK 35
        run: |
          yes | sdkmanager --licenses >/dev/null || true
          sdkmanager "platforms;android-35" "build-tools;35.0.0"

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: "8.9"

      - name: Build SKYNET GPS Bridge
        working-directory: SKYNET_GPS_Bridge_Android
        run: gradle :app:assembleDebug --no-daemon

      - name: Rename APK
        run: |
          cp SKYNET_GPS_Bridge_Android/app/build/outputs/apk/debug/app-debug.apk SKYNET_GPS_Bridge_v2.apk

      - name: Upload SKYNET APK
        uses: actions/upload-artifact@v4
        with:
          name: SKYNET_GPS_Bridge_v2_APK
          path: SKYNET_GPS_Bridge_v2.apk
